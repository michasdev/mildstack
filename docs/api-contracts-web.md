# Web API Contracts

**Part ID:** `web`

The website (`apps/web/www`) is a presentation application and does not expose backend API contracts in this repository.

## Current State

- No server routes are implemented inside `apps/web/www`.
- No direct AWS emulation endpoints are served by this part.
- Integration with runtime APIs is out of scope for current website implementation.

