# Web Data Models

**Part ID:** `web`

The website does not define backend persistence models in this repository.

Current state:

- Client-side UI components and static content only.
- No server-owned database schema.
- No migration folders or ORM models in `apps/web/www`.

